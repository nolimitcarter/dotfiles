new-session 

unbind C-b
set -g prefix C-a
bind-key C-a last-window
set -g status off
bind | split-window -h
bind \ split-window -h 
bind C-\ split-window -h
bind - split-window -v
bind _ split-window -v 
bind C-- split-window -v
setw -g mode-keys vi
